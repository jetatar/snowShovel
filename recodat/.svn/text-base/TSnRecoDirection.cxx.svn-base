#include "TSnRecoDirection.h"

ClassImp(TSnRecoDirection);

void TSnRecoDirection::Print(Option_t* option) const {
   TSnRecoResult::Print(option);
   Printf("Direction:");
   fDir.Print(option);
   Printf("Dir Errs:");
   fErr.Print(option);
   Printf("Fit Quality = %g", fLik);
}

