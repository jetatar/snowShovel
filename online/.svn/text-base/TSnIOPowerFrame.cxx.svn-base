#include "TSnIOPowerFrame.h"

ClassImp(TSnIOPowerFrame);

const UChar_t TSnIOPowerFrame::kIOvers    = 2; // MUST INCREASE if WriteTo changes
const UInt_t  TSnIOPowerFrame::kMaxSizeOf = 
                        sizeof(UChar_t)+(4u*sizeof(Float_t))
                       +sizeof(UInt_t);

UChar_t TSnIOPowerFrame::ReadFromW(const TSnIOBuffer& b, TSnPowerReading& p) {
   // calls the templated function, since rootcint can't
   // parse the template directly.
   // this function will be available in CINT and PyRoot
   return ReadFrom(b, p);
}

UChar_t TSnIOPowerFrame::ReadFromW(const Char_t*& b, TSnPowerReading& p) {
   // calls the templated function, since rootcint can't
   // parse the template directly.
   // this function will be available in CINT and PyRoot
   return ReadFrom(b, p);
}

UChar_t TSnIOPowerFrame::ReadFromW(FILE*& b, TSnPowerReading& p) {
   // calls the templated function, since rootcint can't
   // parse the template directly.
   // this function will be available in CINT and PyRoot
   return ReadFrom(b, p);
}

void TSnIOPowerFrame::WriteToW(TSnIOBuffer& b, const TSnPowerReading& p,
                               const UChar_t Rv) {
   // calls the templated function, since rootcint can't
   // parse the template directly.
   // this function will be available in CINT and PyRoot
   WriteTo(b, p, Rv);
}

void TSnIOPowerFrame::WriteToW(Char_t*& b, const TSnPowerReading& p,
                               const UChar_t Rv) {
   // calls the templated function, since rootcint can't
   // parse the template directly.
   // this function will be available in CINT and PyRoot
   WriteTo(b, p, Rv);
}

void TSnIOPowerFrame::WriteToW(FILE*& b, const TSnPowerReading& p,
                               const UChar_t Rv) {
   // calls the templated function, since rootcint can't
   // parse the template directly.
   // this function will be available in CINT and PyRoot
   WriteTo(b, p, Rv);
}

